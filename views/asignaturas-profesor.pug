extends layout

block content
  h1 Asignaturas Impartidas por Profesor

  h2 Profesor: #{profesor.nombre} #{profesor.apellido} #{profesor.email} #{profesor.telefono}

  if asignaturasImpartidas.length > 0
    ul
      each asignatura in asignaturasImpartidas
        li= asignatura.asignatura
  else
    p El profesor no está impartiendo clases en ninguna asignatura.

  a(href="/impartir") Volver atrás
